#include <bits/stdc++.h>
#define endl "\n"
using namespace std;

#define ln(x) (int)(x).length()
#define sz(x) (int)(x).size()
#define ll long long

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    return 0;
}